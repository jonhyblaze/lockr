{
  "name": "lockr",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @lockr/web",
    "cli": "npm run dev --workspace @lockr/cli",
    "build": "npm run build --workspace @lockr/web",
    "typecheck": "npm run typecheck --workspace @lockr/web"
  },
  "devDependencies": {
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  }
}
